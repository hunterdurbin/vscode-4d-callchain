// LOCKS: ProjectMethod symbol kind. Target for EXECUTE METHOD IN SUBFORM
// resolution — exercises the resolver's ProjectMethod fallback when no
// matching FormObjectMethod (`<form>.Subform_Helper_Target`) exists.
