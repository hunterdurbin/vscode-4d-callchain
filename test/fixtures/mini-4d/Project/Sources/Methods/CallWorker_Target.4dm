// LOCKS: target for CallWorker_DynamicFirstArg.4dm's
//        CALL WORKER(...; "CallWorker_Target"; ...) edge.
//        Workers are procedure-style — they run for side effects, no
//        return value.
#DECLARE($params : Object)
