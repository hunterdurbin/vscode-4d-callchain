// LOCKS: target for OrderForm's `expression: "GetTotal()"` field —
//        proves the form-definition JSON scanner emits a real edge into a
//        project method when the expression is a bare call.
$0:=0
